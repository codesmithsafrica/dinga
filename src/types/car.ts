export interface Car {
  id: number;
  make: string;
  model: string;
  year: number;
  image: string;
  price?: number;
  description?: string;
  ownerName: string;
  phoneNumber: string;
  plateNumber: string;
}
